<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head')%>
<body>
    <%- include('./partials/menu')%>

<center>
        <main>
            <form action="/users" class="form" method="post" enctype="multipart/form-data" >
                 <h1>Registrate</h1>
                 <label for="nombre"><p>Tu nombre</p></label>
                 <input type="text" name="nombre" id="nombre" placeholder="Nombres" value="<%= locals.old && old.nombre ? old.nombre : '' %>">
                 <br>
                 <label for="apellido"><p>Apellido</p></label>
                 <input type="text" name="apellido" id="nombre" placeholder="Apellido" value="<%=locals.old && old.apellido ? old.apellido : ''%>">
                 <br>
                 <label for="usuario"><p>Direccion de correo electronico o numero de telefono</p></label>
                 <input type="email" id="usuario" name="usuario" value="<%=locals.old && old.usuario ? old.usuario: ''%>">
                 <br>
                <label for="image"><p>Introduce tu foto</p></label>
                <input type="file" id="image" name="image" value="//<%=locals.old && old.image ? old.image: '' %>" >
                 <br>
                 <label for="password"><p>Contraseña</p></label>
                 <input type="password" id="password" name="password" placeholder="Como minimo 6 caracteres" value="<%=locals.old && old.password ? old.password : '' %>">
                 <p id="arriba">!La contraseña debe contener al menos 6 caracteres</p>

                 <label for="verificarcontraseña"><p >Vuelve a repetir la contraseña</p></label>
                 <input type="password" name="verificaPassword" id="verifiquedpassword" value="<%=locals.old && old.verificaPassword ? old.verificaPassword : '' %>" >
                 <br>
                    <%if(locals.errors) 
                    {
                        errors.forEach(errors => {%>
                    <p class="arriba"><%=errors.msg%></p>
                    <% })
                    }
                    %>
                 <br>
                 
                 <input type="submit" value="Continuar">
                 <br>   
                 <p>Al crear una cuenta, aceptas las <u><a href="">Condiciones de uso</a></u> y el <u><a href="">Aviso de privacidad</a></u> de E-Books.</p>
                 <br>
                 
                 <p>Ya tienes cuenta? <u><a href="">Inicia sesion</a></u></p>
                </form>
            <br>
            <br>

        </main>

    </center>
   
    <%- include('./partials/footer')%>

</body>
</html>
